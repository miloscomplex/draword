development:
  adapter: async
    # adapter: redis
    # url: <%= ENV.fetch("REDIS_URL") { "redis://localhost:6379/1" } %>
    # channel_prefix: backend_production


test:
  adapter: test

production:
  adapter: redis
  url: <%= ENV.fetch("REDIS_URL") { "redis://git.heroku.com/safe-earth-83403/" } %>
  # channel_prefix: backend_production
